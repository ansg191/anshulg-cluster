apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: kanidm
  labels:
    app: kanidm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kanidm
  serviceName: kanidm
  template:
    metadata:
      name: kanidm
      labels:
        app: kanidm
      annotations:
        gke-gcsfuse/volumes: "true"
    spec:
      serviceAccountName: kanidm
      containers:
        - name: kanidm
          image: docker.io/kanidm/server:1.4.4@sha256:423baa48c4cb0e41642d88772aa5d170daac69c827392a00f96de6a387032745
          imagePullPolicy: IfNotPresent
          env: []
          ports:
            - containerPort: 8443
              protocol: TCP
            - containerPort: 3636
              protocol: TCP
          volumeMounts:
            - mountPath: /data
              name: data
            - mountPath: /data/server.toml
              name: config
              subPath: server.toml
              readOnly: true
            - mountPath: /certs
              name: certs
            - mountPath: /backups
              name: backups
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ "ALL" ]
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 256Mi
      restartPolicy: Always
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        fsGroup: 1000
      nodeSelector:
        cloud.google.com/gke-provisioning: standard
      priorityClassName: high-priority
      volumes:
        - name: config
          configMap:
            name: kanidm-config
            defaultMode: 0440
        - name: certs
          secret:
            secretName: kanidm-cert-tls
            defaultMode: 0440
        - name: backups
          csi:
            driver: gcsfuse.csi.storage.gke.io
            readOnly: false
            volumeAttributes:
              bucketName: "kanidm-backup-bucket-wzqjn"
              gcsfuseLoggingSeverity: warning
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 4Gi
