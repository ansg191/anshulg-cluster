apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: protonmail-bridge-pvc
spec:
  storageClassName: standard-rwo
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
