apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: miniflux-cluster
spec:
  instances: 3
  bootstrap:
    initdb:
      database: miniflux
      owner: miniflux
  storage:
    size: 8Gi
