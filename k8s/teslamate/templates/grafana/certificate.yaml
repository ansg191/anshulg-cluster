{{- if and .Values.grafana.ingressRoute.enabled .Values.grafana.ingressRoute.enabled -}}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "teslamate.fullname" . }}-grafana-tls
spec:
  secretName: {{ include "teslamate.fullname" . }}-grafana-tls
  {{- with (first .Values.grafana.ingressRoute.hosts) }}
  commonName: {{ . | quote }}
  {{- end }}
  {{- with .Values.grafana.ingressRoute.hosts }}
  dnsNames:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  duration: {{ .Values.grafana.ingressRoute.tls.duration }}
  renewBefore: {{ .Values.grafana.ingressRoute.tls.renewBefore }}
  {{- with .Values.grafana.ingressRoute.tls.privateKey }}
  privateKey:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  subject:
    organizations:
      - Anshul Gupta
    organizationalUnits:
      - k8s
    provinces:
      - California
    countries:
      - US
  {{- with .Values.grafana.ingressRoute.tls.issuerRef }}
  issuerRef:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
