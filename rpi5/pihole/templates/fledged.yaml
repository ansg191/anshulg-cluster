apiVersion: kubefledged.io/v1alpha2
kind: ImageCache
metadata:
  name: pihole-cache
  labels:
    app: pihole
    kubefledged: imagecache
spec:
  cacheSpec:
    - images:
        - pihole/pihole:2024.05.0
